#!/bin/sh
# Shim `tvm-ffi-config` (non prodotto da cmake): tvm-ffi-sys/build.rs lo invoca
# per trovare libtvm_ffi.so. Metterlo su PATH quando si compila; override TVM_FFI_LIBDIR.
LIBDIR="${TVM_FFI_LIBDIR:-/home/ltrubbiani/tvm/src/tvm-current/build/lib}"
echo "$LIBDIR"
